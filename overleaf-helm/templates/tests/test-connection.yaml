apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "overleaf-helm.fullname" . }}-test-connection"
  labels:
    {{- include "overleaf-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "overleaf-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
